* filter the brandlancer list to exclude clients, only brandlancers should show

* change all urls to generic ones, so they work on the deployment server

* change db from sqlite to mysql, or whichever one you'll use on the server.

* read on deployment for django.

* fix the render/redirect thing. redirect isn't working, so we're using render as c/o